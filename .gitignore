# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# Environments
.venv/
venv/
env/
.env
.env.*

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# detguard run artifacts (results are regenerated; a CLIENT's baseline is
# theirs to commit and this rule does not generalise to it — see below)
results*.json
ci_report*.json
audit*.jsonl
runs/

# This repo's own demo corpus and its baseline, both regenerated by
# `detguard corpus build`/`baseline snapshot` against examples/banking_agent.
# Not a template for a client's repo: a client's corpus/baseline is a
# versioned artifact and belongs in git.
/corpus/attacks/
/corpus/baseline.json
/ci_report.md
